# URL do servidor local do LM Studio
LM_STUDIO_URL="http://localhost:1234"

# Endpoint da API do LM Studio (ex: /v1/chat/completions ou /v1/completions)
# Use /v1/chat/completions para modelos de chat mais recentes.
# Use /v1/completions para modelos de texto legado.
LM_STUDIO_API_ENDPOINT="/v1/completions"

# Identificador do modelo carregado no LM Studio.
# O valor "local-model" instrui o LM Studio a usar qualquer modelo que esteja carregado no momento.
LM_STUDIO_MODEL="local-model"